#include "cpu_to_gpu_types.h"

#ifndef LOGGER_IGNORE_ASSERTS
void validate_framedata(
    GPUVertex * vertices,
    uint32_t vertices_size)
{
    for (
        uint32_t i = 0;
        i < vertices_size;
        i++)
    {
    }
}
#endif
